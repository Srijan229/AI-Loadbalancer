apiVersion: v1
kind: Service
metadata:
  name: metrics-collector
  labels:
    app: metrics-collector
spec:
  selector:
    app: metrics-collector
  ports:
    - name: http
      port: 8004
      targetPort: http
