# Shared Contracts

This directory will hold Python request and response models shared across services.

Initial shared model groups:
- gateway payloads
- worker payloads
- orchestrator policy models
- forecast models

The goal is to keep service interfaces explicit and versionable.

